import React from "react";

const Clearingvisitewebsite = ({ showCount }: any) => {
  return (
      <button type="button" className={`btn btn-blue min-w-[160px] w-full`}>Visite website</button>
  );
};

export default Clearingvisitewebsite;
